/**
 * Build a URL to call the API with, making sure everything is encoded
 * @param base
 * @param parts any additional path components
 * @hidden
 */
export declare function buildURL(base: string, ...parts: string[]): string;
